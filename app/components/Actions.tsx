'use client';
import React from 'react';
import IconButton from './IconButton';

export default function Actions() {
  return (
    <div>
      <IconButton icon="ri-mic-line" onPress={() => ''} />
    </div>
  );
}
